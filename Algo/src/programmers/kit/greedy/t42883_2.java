package programmers.kit.greedy;

//2012.12.04 
// :: 프로그래머스 greedy - 큰수만들기
public class t42883_2 {
	// 모든 number 에대한 멱집합을 만들고 배열에 저장할까?
	// number.length - k 길이 인것중에서
	// 최대값을 찾을까?
	public String solution(String number, int k) {
		String answer = "";
		boolean[] visited = new boolean[number.length()];
		return answer;
	}

	// 멱집합 모든 경우의 수를 가진 배열 만들기
	public String[] allnumber(String number, int index) {
		String[] ans = {};
		if(index==number.length()&&(visited)) {
			ans[index++]=
		}
		return ans;
	}

	public static void main(String[] args) {

	}

}
